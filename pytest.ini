[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    functional: end-to-end pipeline tests
    accuracy: AI output quality tests  
    robustness: edge case and security tests
    unit: unit tests for individual components
timeout = 300
