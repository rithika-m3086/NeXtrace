# ── Core ────────────────────────────────────────────────────────────────────
pydantic>=2.0
python-dotenv
openai                 # client used against AI/ML API (partner provider) base_url

# ── Band multi-agent coordination layer ─────────────────────────────────────
# Official Band SDK. Renamed from thenvoi-sdk in v1.0.0.
# Imports from band.client.rest (RestClient, ChatMessageRequest, etc.)
band-sdk>=1.0.0

# ── UI ──────────────────────────────────────────────────────────────────────
streamlit

# ── Premium features ────────────────────────────────────────────────────────
requests               # GeoIP lookups + GitHub/Jira ticket export
reportlab              # PDF incident report export

# ── Tests ───────────────────────────────────────────────────────────────────
pytest
pytest-asyncio
